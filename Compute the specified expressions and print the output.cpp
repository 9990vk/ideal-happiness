#include <iostream>
using namespace std;

int main() 
{
    float a,b,c;
    a=(25.5 * 3.5 - 3.5 * 3.5);
    b=(40.5 - 4.5);
    c=a/b;
 cout << "Result of the expression (25.5 * 3.5 - 3.5 * 3.5) / (40.5 - 4.5) is :" << c ;
  return 0;
}